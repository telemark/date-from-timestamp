{
  "name": "date-from-timestamp",
  "description": "Date on format dd.mm.yyyy from timestamp",
  "version": "2.0.1",
  "license": "MIT",
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "keywords": [
    "date",
    "convert",
    "tfk",
    "Telemark fylkeskommune"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/telemark/date-from-timestamp.git"
  },
  "bugs": {
    "url": "https://github.com/telemark/date-from-timestamp/issues"
  },
  "homepage": "https://github.com/telemark/date-from-timestamp#readme",
  "engines": {
    "node": ">=8.15.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "standard && npm audit && tap --reporter=spec test/**/*.js",
    "coverage": "tap test/**/*.js --coverage",
    "coveralls": "tap --cov --coverage-report=lcov test/**/*.js && cat coverage/lcov.info | coveralls",
    "standard-fix": "standard --fix",
    "refresh": "rm -rf node_modules && rm package-json && npm install"
  },
  "devDependencies": {
    "standard": "12.0.1",
    "coveralls": "3.0.3",
    "tap": "14.2.0"
  }
}
